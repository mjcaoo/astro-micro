---
import Layout from "@layouts/Layout.astro";
import Container from "@components/Container.astro";
import { SITE, HOME, SOCIALS } from "@consts";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";

// blogs
const blog = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_POSTS_ON_HOMEPAGE);
// projects
const projects: CollectionEntry<"projects">[] = (
  await getCollection("projects")
)
  .filter((project) => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_PROJECTS_ON_HOMEPAGE);
---

<Layout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <aside data-pagefind-ignore>
      <h1 class="font-semibold text-black dark:text-white">
        介绍
      </h1>
      <div class="space-y-16">
        <!-- Introduction -->
        <section>
          <article class="space-y-4">
            <span>
              <p>
                我是Mjcaoo。目前就读于安徽工程大学计算机科学与技术专业。
                我是一名热衷于 <Link href="https://github.com/mjcaoo">计算机视觉与机器人开发</Link> 的开发者。
                擅长使用 Python 进行深度学习模型（YOLO系列）的搭建与调优，并熟悉 ROS2 机器人开发框架。
              </p>
              <p>
                我有丰富的 <Link href="/projects">竞赛与项目经验</Link> ，
                曾带领团队获得全国大学生智能汽车竞赛<strong>全国二等奖</strong>，并开发过基于CV的校园车辆治理系统。
              </p>
            </span>
            <span class="animate">
              <p>
                在代码之外，我注重工程规范与代码的可读性。
                目前正在探索自动驾驶与智能辅助系统的开发与测试。
                欢迎访问我的 <Link href="https://github.com/mjcaoo">GitHub</Link>
                查看我的开源项目，或通过邮件与我交流。
              </p>
            </span>
          </article>
        </section>
        <!-- posts -->
        <section class="animate space-y-6">
          <div class="flex flex-wrap items-center justify-between gap-y-2">
            <h2 class="font-semibold text-black dark:text-white">
              最近文章
            </h2>
            <Link href="/blog"> See all posts </Link>
          </div>
          <ul class="not-prose flex flex-col gap-4">
            {
              blog.map((post) => (
                <li>
                  <ArrowCard entry={post} />
                </li>
              ))
            }
          </ul>
        </section>
        <!-- projects -->
        <section class="animate space-y-6">
          <div class="flex flex-wrap items-center justify-between gap-y-2">
            <h2 class="font-semibold text-black dark:text-white">
              近期项目
            </h2>
            <Link href="/projects"> See all projects </Link>
          </div>
          <ul class="not-prose flex flex-col gap-4">
            {
              projects.map((project) => (
                <li>
                  <ArrowCard entry={project} />
                </li>
              ))
            }
          </ul>
        </section>
        <!-- Let's Connect -->
        <section class="animate space-y-4">
          <h2 class="font-semibold text-black dark:text-white">
            联系我
          </h2>
          <article>
            <p>
              如果您有任何事宜需要沟通，或只是想建立联系，请通过社交媒体或邮件留言。
            </p>
          </article>
          <ul class="not-prose flex flex-wrap gap-2">
            {
              SOCIALS.map((SOCIAL) => (
                <li class="flex gap-x-2 text-nowrap">
                  <Link
                    href={SOCIAL.HREF}
                    external
                    aria-label={`${SITE.TITLE} on ${SOCIAL.NAME}`}
                  >
                    {SOCIAL.NAME}
                  </Link>
                  {"/"}
                </li>
              ))
            }
            <li class="line-clamp-1">
              <Link
                href={`mailto:${SITE.EMAIL}`}
                aria-label={`Email ${SITE.TITLE}`}
              >
                {SITE.EMAIL}
              </Link>
            </li>
          </ul>
        </section>
      </div>
    </aside>
  </Container>
</Layout>
